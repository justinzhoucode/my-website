# my website

This is my personal site (lowkey vibecoded)
It's React + Vite + TypeScript + Tailwind with a little WebGL background, and it
does the one thing it needs to do.

## run it

```bash
npm install
npm run dev
```

Then open the URL it prints (usually http://localhost:5173).

That's it.
